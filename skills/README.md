# Skills Catalog

Rule: **1 skill = 1 file**.

File naming:
- `skills/<scope>/<name>.md`
- The filename MUST match the frontmatter `name`.

Selection:
- Pick skills by trigger keywords.
- If multiple apply, the most specific wins.
- If conflict: security > privacy/data > reliability > testing > architecture > DX > UI.
